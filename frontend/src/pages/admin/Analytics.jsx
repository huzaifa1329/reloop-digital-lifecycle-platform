import { Bar,BarChart,CartesianGrid,ResponsiveContainer,Tooltip,XAxis,YAxis } from "recharts";import PageContainer from "../../components/layout/PageContainer";import SectionHeading from "../../components/common/SectionHeading";import Card from "../../components/common/Card";import { useData } from "../../context/DataContext";
function Analytics(){const {analytics}=useData();const m=analytics?.metrics||{};const metrics=[["Users",m.users||0],["Customers",Math.max((m.users||0)-(m.providers||0),0)],["Verified providers",m.providers||0],["Products",m.products||0],["Active listings",m.listings||0],["Donations",m.donations||0],["Recycling requests",m.recycling||0]];return <PageContainer className="space-y-8 py-8"><SectionHeading eyebrow="Analytics" title="Platform Analytics" description="Operational insights calculated directly from MongoDB."/><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{metrics.map(([l,v])=><Card key={l}><p className="font-mono text-[10px] uppercase tracking-[.15em] text-reloop-espresso/40">{l}</p><p className="mt-3 font-display text-3xl font-bold">{v}</p></Card>)}</div><Card><h3 className="font-display text-lg font-bold">Repair request distribution</h3><div className="mt-5 h-80">{analytics?.repairData?.length?<ResponsiveContainer width="100%" height="100%"><BarChart data={analytics.repairData}><CartesianGrid strokeDasharray="3 3" stroke="#e6ded3"/><XAxis dataKey="status" tick={{fontSize:10}}/><YAxis allowDecimals={false}/><Tooltip/><Bar dataKey="count" fill="#e85d3f" radius={[6,6,0,0]}/></BarChart></ResponsiveContainer>:<div className="flex h-full items-center justify-center text-sm text-reloop-espresso/40">No repair data yet.</div>}</div></Card></PageContainer>}
export default Analytics;
