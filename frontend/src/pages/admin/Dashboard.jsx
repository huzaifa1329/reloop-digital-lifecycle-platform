import { Bar, BarChart, CartesianGrid, Cell, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import PageContainer from "../../components/layout/PageContainer";import SectionHeading from "../../components/common/SectionHeading";import Card from "../../components/common/Card";import { useData } from "../../context/DataContext";
const COLORS=["#e85d3f","#c8e34a","#c98b6b","#211a17","#8c8179"];
function Dashboard(){const {dashboard,loading}=useData();const m=dashboard?.metrics||{};const lifecycle=dashboard?.lifecycleData||[];const repair=dashboard?.repairData||[];const metrics=[['Total Users',m.users||0],['Repair Partners',m.providers||0],['Registered Products',m.products||0],['Repair Requests',m.repairs||0],['Marketplace Listings',m.listings||0],['Pending Verifications',m.pending||0]];return <PageContainer className="space-y-8 py-8"><SectionHeading eyebrow="Admin" title="Platform Overview" description="Live metrics from MongoDB. The dashboard refreshes automatically while the app is open."/><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{metrics.map(([label,value])=><Card key={label}><p className="font-mono text-[10px] uppercase tracking-[0.15em] text-reloop-espresso/40">{label}</p><p className="mt-3 font-display text-3xl font-bold">{loading?'…':value}</p></Card>)}</div><div className="grid gap-6 lg:grid-cols-2"><Card><h3 className="font-display text-lg font-bold">Lifecycle Distribution</h3><div className="mt-4 h-72">{lifecycle.length?<ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={lifecycle} dataKey="value" nameKey="name" innerRadius={55} outerRadius={90} paddingAngle={2}>{lifecycle.map((x,i)=><Cell key={x.name} fill={COLORS[i%COLORS.length]}/>)}</Pie><Tooltip/></PieChart></ResponsiveContainer>:<div className="flex h-full items-center justify-center text-sm text-reloop-espresso/40">No lifecycle data yet.</div>}</div></Card><Card><h3 className="font-display text-lg font-bold">Repair Activity</h3><div className="mt-4 h-72">{repair.length?<ResponsiveContainer width="100%" height="100%"><BarChart data={repair}><CartesianGrid strokeDasharray="3 3" stroke="#e6ded3"/><XAxis dataKey="name" tick={{fontSize:10}}/><YAxis allowDecimals={false} tick={{fontSize:10}}/><Tooltip/><Bar dataKey="value" fill="#e85d3f" radius={[6,6,0,0]}/></BarChart></ResponsiveContainer>:<div className="flex h-full items-center justify-center text-sm text-reloop-espresso/40">No repair activity yet.</div>}</div></Card></div></PageContainer>}
export default Dashboard;
